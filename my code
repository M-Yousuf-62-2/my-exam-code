#include <iostream>
#include <string>
using namespace std;

int main() {
    string word, line;
    
    cout << "Enter a phrase: ";
    cin >> word;              
    cin.ignore();        
    getline(cin, line);    
    
    cout << "cin reads: " << word << ", getline reads: " << line << endl;
    return 0;
}